[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # This tells Netlify to stop scanning for secrets and failing the build.
  # Firebase NEXT_PUBLIC keys are meant to be public, so this is safe.
  SECRETS_SCAN_ENABLED = "false"